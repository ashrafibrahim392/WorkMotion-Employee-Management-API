server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password: password
    driverClassName: org.h2.Driver
    console.enabled: true
  jpa:
    hibernate.ddl-auto: update
    generate-ddl: true
    show-sql: true
